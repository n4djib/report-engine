FROM node:20-alpine

# Enable pnpm
RUN corepack enable

WORKDIR /app

# Copy only lock + workspace for install layer caching
COPY pnpm-lock.yaml pnpm-workspace.yaml package.json ./

# Install deps
RUN pnpm install

# Keep container alive (actual command overridden by compose)
CMD ["sh"]