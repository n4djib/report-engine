docker compose up -d


= server:
nats pub test "hello"

nats sub test -s nats://localhost:4222

= leaf
nats pub test "hello" -s nats://localhost:4223

nats sub test -s nats://localhost:4223


= reload config config inside docker


= Display logs
docker logs nats-hub

check the logs from thye service (container) nats-hub

docker logs nats-leaf1

docker logs nats-leaf2 -f
    -f will start watching the log

